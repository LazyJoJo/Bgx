<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stock.fund.infrastructure.mapper.alert.AlertHistoryMapper">

    <select id="findByUserId" parameterType="long" resultType="com.stock.fund.infrastructure.entity.alert.AlertHistoryPO">
        SELECT * FROM alert_history WHERE user_id = #{userId} ORDER BY triggered_at DESC
    </select>

    <select id="findByUserIdAndAlertId" resultType="com.stock.fund.infrastructure.entity.alert.AlertHistoryPO">
        SELECT * FROM alert_history 
        WHERE user_id = #{userId} AND alert_id = #{alertId}
        ORDER BY triggered_at DESC
    </select>

    <select id="findByUserIdAndDateRange" resultType="com.stock.fund.infrastructure.entity.alert.AlertHistoryPO">
        SELECT * FROM alert_history 
        WHERE user_id = #{userId} 
        AND triggered_at BETWEEN #{start} AND #{end}
        ORDER BY triggered_at DESC
    </select>

</mapper>