<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stock.fund.infrastructure.mapper.riskalert.RiskAlertDetailMapper">

    <!-- 根据 risk_alert_id 查询所有明细 -->
    <select id="findByRiskAlertId" resultType="com.stock.fund.infrastructure.entity.riskalert.RiskAlertDetailPO">
        SELECT id, risk_alert_id, symbol, change_percent, current_price,
               triggered_at, trigger_reason, time_point
        FROM risk_alert_detail
        WHERE risk_alert_id = #{riskAlertId}
        ORDER BY triggered_at DESC
    </select>

    <!-- 根据 risk_alert_id 删除所有明细 -->
    <delete id="deleteByRiskAlertId">
        DELETE FROM risk_alert_detail
        WHERE risk_alert_id = #{riskAlertId}
    </delete>

</mapper>
