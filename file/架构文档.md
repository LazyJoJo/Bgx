# Bgx 股票/基金数据系统架构文档

## 1. 项目概述

### 1.1 项目简介

Bgx是一个金融数据分析与提醒系统，主要功能包括：
- 股票和基金数据的采集、存储与展示
- 基于价格和涨跌幅的价格提醒功能
- 风险提醒自动检测（工作日11:30和14:30触发）
- 基金分析功能

### 1.2 项目结构

```
D:\workspace\Bgx
├── app/                          # 前端应用 (React + TypeScript + Vite)
│   ├── src/
│   │   ├── components/           # React 组件
│   │   ├── pages/                # 页面组件
│   │   ├── services/api/          # API 服务层
│   │   ├── store/                # Redux 状态管理
│   │   ├── types/                # TypeScript 类型定义
│   │   └── assets/               # 静态资源
│   └── package.json
├── backend/                      # 后端应用 (Spring Boot 3.1.5 + JDK 21)
│   ├── src/main/java/com/stock/fund/
│   │   ├── application/          # 应用层 (服务、调度器)
│   │   ├── domain/               # 领域层 (实体、仓储接口)
│   │   ├── infrastructure/       # 基础设施层 (PO、Mapper、仓储实现)
│   │   ├── interfaces/           # 接口层 (Controller、DTO)
│   │   ├── aspect/               # 切面 (日志、性能、异常处理)
│   │   └── config/               # 配置类
│   └── pom.xml
└── 数据库设计.md                  # 数据库设计文档
```

---

## 2. 技术架构

### 2.1 技术栈概览

| 层级 | 技术 | 版本 |
|------|------|------|
| **前端框架** | React | 18.2 |
| **前端语言** | TypeScript | 5.2 |
| **前端构建** | Vite | 5.1 |
| **前端 UI** | Ant Design | 5.14 |
| **状态管理** | Redux Toolkit | 2.2 |
| **图表** | ECharts | 5.5 |
| **后端框架** | Spring Boot | 3.1.5 |
| **后端语言** | JDK | 21 |
| **ORM** | MyBatis Plus | 3.5.3.1 |
| **数据库** | PostgreSQL | - |
| **缓存** | Caffeine | 3.1.8 |
| **HTTP 客户端** | OkHttp | 4.12 |
| **API 文档** | SpringDoc OpenAPI | 2.2.0 |

### 2.2 架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         前端 (React + Vite)                      │
├─────────────────────────────────────────────────────────────────┤
│  Pages          │  Components       │  Store (Redux Toolkit)   │
│  - Dashboard     │  - AlertConfigForm│  - alertsSlice           │
│  - StockList    │  - SymbolSelector │  - stocksSlice           │
│  - FundList     │  - BatchResultModal│  - fundsSlice            │
│  - AlertCreate  │  - Header/Sidebar │  - riskAlertsSlice       │
│  - RiskAlertList│  - BatchSubscribe  │  - notificationsSlice    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼ HTTP/HTTPS
┌─────────────────────────────────────────────────────────────────┐
│                     后端 (Spring Boot 3.1.5)                    │
├─────────────────────────────────────────────────────────────────┤
│  Interfaces Layer (REST API)                                    │
│  ├── AlertController        (/api/alerts)                       │
│  ├── RiskAlertController    (/api/risk-alerts)                  │
│  ├── FundAnalysisController (/api/fund-analysis)               │
│  └── DataCollectionController                                   │
├─────────────────────────────────────────────────────────────────┤
│  Application Layer (Business Logic)                              │
│  ├── AlertAppService       - 价格提醒业务逻辑                    │
│  ├── RiskAlertAppService   - 风险提醒业务逻辑                    │
│  ├── DataCollectionAppService - 数据采集                        │
│  └── Schedulers            - 定时任务调度                        │
├─────────────────────────────────────────────────────────────────┤
│  Domain Layer (Entities & Repository Interfaces)                │
│  ├── PriceAlert, AlertHistory, RiskAlert                       │
│  ├── Stock, Fund, StockQuote, FundQuote                        │
│  └── DataCollectionTarget                                       │
├─────────────────────────────────────────────────────────────────┤
│  Infrastructure Layer (MyBatis Plus)                            │
│  ├── PO (StockBasicPO, FundBasicPO, PriceAlertPO, etc.)        │
│  ├── Mapper (StockBasicMapper, PriceAlertMapper, etc.)         │
│  └── Repository Implementations                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      PostgreSQL Database                         │
│  stock_basic, fund_basic, stock_quote, fund_quote              │
│  price_alert, alert_history, user_subscription, risk_alert      │
│  data_collection_target                                         │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 前端架构详情

### 3.1 目录结构

```
app/src/
├── main.tsx                 # 入口文件
├── App.tsx                  # 根组件 (路由配置)
├── components/
│   ├── ErrorBoundary.tsx    # 错误边界
│   ├── alerts/
│   │   ├── AlertConfigForm.tsx    # 提醒配置表单
│   │   ├── BatchResultModal.tsx   # 批量结果弹窗
│   │   └── SymbolSelector.tsx      # 标的选择器
│   ├── layout/
│   │   ├── Header.tsx       # 页头
│   │   └── Sidebar.tsx      # 侧边栏
│   └── risk-alerts/
│       └── BatchSubscribeModal.tsx  # 批量订阅弹窗
├── pages/
│   ├── dashboard/
│   │   └── Dashboard.tsx     # 仪表盘
│   ├── stocks/
│   │   └── StockList.tsx    # 股票列表
│   ├── funds/
│   │   ├── FundList.tsx     # 基金列表
│   │   └── FundAnalysis.tsx # 基金分析
│   ├── alerts/
│   │   ├── AlertList.tsx    # 提醒列表
│   │   └── AlertCreate.tsx  # 创建/编辑提醒
│   └── risk-alerts/
│       └── RiskAlertList.tsx  # 风险提醒列表
├── services/api/
│   ├── client.ts            # Axios 客户端封装
│   ├── alerts.ts            # 提醒 API
│   ├── stocks.ts            # 股票 API
│   ├── funds.ts             # 基金 API
│   ├── riskAlerts.ts        # 风险提醒 API
│   └── dashboard.ts         # 仪表盘 API
├── store/
│   ├── index.ts             # Redux Store 配置
│   ├── hooks.ts             # Typed hooks
│   └── slices/
│       ├── alertsSlice.ts    # 提醒状态
│       ├── stocksSlice.ts    # 股票状态
│       ├── fundsSlice.ts     # 基金状态
│       ├── riskAlertsSlice.ts # 风险提醒状态
│       └── notificationsSlice.ts # 通知状态
└── types/
    └── index.ts             # 类型定义
```

### 3.2 路由配置

```typescript
// App.tsx 路由配置
<Routes>
  <Route path="/" element={<Dashboard />} />
  <Route path="/dashboard" element={<Dashboard />} />
  <Route path="/stocks" element={<StockList />} />
  <Route path="/funds" element={<FundList />} />
  <Route path="/fund-analysis" element={<FundAnalysis />} />
  <Route path="/alerts" element={<AlertList />} />
  <Route path="/alerts/edit/:id" element={<AlertCreate />} />
  <Route path="/alerts/create" element={<AlertCreate />} />
  <Route path="/risk-alerts" element={<RiskAlertList />} />
</Routes>
```

### 3.3 状态管理 (Redux Toolkit)

```typescript
// store/index.ts
export const store = configureStore({
  reducer: {
    stocks: stocksReducer,
    funds: fundsReducer,
    alerts: alertsReducer,
    riskAlerts: riskAlertsReducer,
    notifications: notificationsReducer,
  },
})

export type RootState = ReturnType<typeof store.getState>
export type AppDispatch = typeof store.dispatch
```

### 3.4 API 客户端

```typescript
// services/api/client.ts
class ApiClient {
  private client: AxiosInstance

  constructor() {
    this.client = axios.create({
      baseURL: '/api',
      timeout: 10000,
      headers: { 'Content-Type': 'application/json' },
    })

    // 请求拦截器：添加认证 token
    this.client.interceptors.request.use((config) => {
      const token = localStorage.getItem('auth_token')
      if (token) {
        config.headers.Authorization = `Bearer ${token}`
      }
      return config
    })

    // 响应拦截器：统一错误处理
    this.client.interceptors.response.use(
      (response) => response.data,
      (error) => {
        if (error.response?.status === 401) {
          localStorage.removeItem('auth_token')
          window.location.href = '/login'
        }
        return Promise.reject(error)
      }
    )
  }
}
```

### 3.5 代理配置 (Vite)

```typescript
// vite.config.ts
server: {
  port: 8080,
  proxy: {
    '/api': {
      target: 'https://localhost:9090',
      changeOrigin: true,
    }
  }
}
```

---

## 4. 后端架构详情

### 4.1 分层架构

```
backend/src/main/java/com/stock/fund/
├── Application.java           # Spring Boot 启动类
├── application/              # 应用层
│   ├── scheduler/           # 定时任务
│   │   ├── AlertScheduler.java         # 价格提醒调度器
│   │   ├── RiskAlertScheduler.java     # 风险提醒调度器
│   │   ├── DataCollectionScheduler.java # 数据采集调度器
│   │   └── SchedulerConfig.java
│   └── service/              # 应用服务
│       ├── alert/
│       │   ├── AlertAppService.java
│       │   ├── impl/AlertAppServiceImpl.java
│       │   └── dto/
│       ├── riskalert/
│       │   ├── RiskAlertAppService.java
│       │   └── impl/RiskAlertAppServiceImpl.java
│       └── DataCollectionAppService.java
├── domain/                   # 领域层
│   ├── entity/              # 领域实体 (聚合根)
│   │   ├── AggregateRoot.java
│   │   ├── Stock.java
│   │   ├── Fund.java
│   │   ├── StockQuote.java
│   │   ├── FundQuote.java
│   │   ├── alert/
│   │   │   ├── PriceAlert.java
│   │   │   └── AlertHistory.java
│   │   └── riskalert/RiskAlert.java
│   └── repository/          # 仓储接口
│       ├── StockRepository.java
│       ├── FundRepository.java
│       └── alert/PriceAlertRepository.java
├── infrastructure/           # 基础设施层
│   ├── entity/             # 持久化对象 (PO)
│   │   ├── StockBasicPO.java
│   │   ├── FundBasicPO.java
│   │   └── alert/PriceAlertPO.java
│   ├── mapper/             # MyBatis Mapper
│   │   ├── StockBasicMapper.java
│   │   └── alert/PriceAlertMapper.java
│   └── repository/          # 仓储实现
│       ├── StockRepositoryImpl.java
│       └── alert/PriceAlertRepositoryImpl.java
├── interfaces/               # 接口层
│   ├── controller/          # REST Controller
│   │   ├── alert/AlertController.java
│   │   └── riskalert/RiskAlertController.java
│   ├── dto/
│   │   ├── request/
│   │   └── response/
│   │       └── ApiResponse.java
│   └── advice/
│       └── GlobalExceptionHandler.java
├── aspect/                   # 切面
│   ├── ExceptionHandlingAspect.java
│   ├── LoggingAspect.java
│   └── PerformanceMonitoringAspect.java
└── config/                   # 配置类
    ├── CacheConfig.java
    ├── DataSourceConfig.java
    └── OkHttpClientConfig.java
```

### 4.2 关键模块说明

#### 4.2.1 提醒模块 (Alert)

**实体：PriceAlert**
```java
public class PriceAlert extends AggregateRoot<Long> {
    private Long userId;
    private String symbol;        // 标的代码
    private String symbolType;    // STOCK / FUND
    private String symbolName;
    private String alertType;     // PRICE_ABOVE / PRICE_BELOW / PERCENTAGE_CHANGE
    private Double targetPrice;
    private Double targetChangePercent;
    private Double currentValue;
    private Double basePrice;
    private String status;        // ACTIVE / TRIGGERED / INACTIVE
    private LocalDateTime lastTriggered;
}
```

**核心接口：AlertController**
| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/api/alerts` | 创建提醒 |
| POST | `/api/alerts/unified` | 统一创建提醒 |
| POST | `/api/alerts/batch` | 批量创建提醒 |
| POST | `/api/alerts/batch/v2` | 批量创建V2 |
| POST | `/api/alerts/check-duplicates` | 检测重复 |
| PUT | `/api/alerts/{id}` | 更新提醒 |
| DELETE | `/api/alerts/{id}` | 删除提醒 |
| GET | `/api/alerts` | 分页查询 |
| GET | `/api/alerts/{id}` | 获取单个 |
| GET | `/api/alerts/user/{userId}` | 获取用户所有 |
| GET | `/api/alerts/user/{userId}/active` | 获取激活的 |
| PATCH | `/api/alerts/{id}/activate` | 激活 |
| PATCH | `/api/alerts/{id}/deactivate` | 停用 |

#### 4.2.2 风险提醒模块 (RiskAlert)

**实体：RiskAlert**
```java
public class RiskAlert extends AggregateRoot<Long> {
    private Long userId;
    private String symbol;
    private String symbolType;      // STOCK / FUND
    private String symbolName;
    private LocalDate alertDate;     // 风险日期
    private String timePoint;        // 11:30 / 14:30
    private Boolean hasRisk;         // 是否触发风险
    private BigDecimal changePercent;
    private BigDecimal currentPrice;
    private BigDecimal yesterdayClose;
    private Boolean isRead;
    private LocalDateTime triggeredAt;
}
```

**核心接口：RiskAlertController**
| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/risk-alerts/user/{userId}` | 获取风险提醒列表（按日聚合） |
| GET | `/api/risk-alerts/today` | 获取今日风险提醒 |
| GET | `/api/risk-alerts/user/{userId}/unread-count` | 未读数量 |
| PATCH | `/api/risk-alerts/{id}/read` | 标记已读 |
| POST | `/api/risk-alerts/user/{userId}/mark-read` | 全部标记已读 |
| POST | `/api/risk-alerts/check` | 手动触发检测（后端定时任务） |
| POST | `/api/risk-alerts/subscribe` | 批量订阅（创建user_subscription） |

**实体：UserSubscription（新增）**
```java
public class UserSubscription extends AggregateRoot<Long> {
    private Long userId;
    private String symbol;
    private String symbolType;      // STOCK / FUND
    private String symbolName;
    private BigDecimal targetChangePercent;  // 目标涨跌幅阈值
    private String status;          // ACTIVE / INACTIVE
    private LocalDateTime createdAt;
    private LocalDateTime updatedAt;
}
```

**核心接口：SubscriptionController（新增）**
| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/subscriptions/user/{userId}` | 获取用户订阅列表 |
| POST | `/api/subscriptions` | 创建订阅 |
| POST | `/api/subscriptions/batch` | 批量创建订阅 |
| DELETE | `/api/subscriptions/{id}` | 删除订阅 |
| PATCH | `/api/subscriptions/{id}/activate` | 启用订阅 |
| PATCH | `/api/subscriptions/{id}/deactivate` | 停用订阅 |

#### 4.2.3 调度任务

| 调度器 | cron 表达式 | 功能 |
|--------|-------------|------|
| AlertScheduler | `${data.collection.schedule.alert-check-cron}` | 定时检查提醒触发 |
| RiskAlertScheduler | 11:30 / 14:30 (工作日) | 风险提醒检测 |
| DataCollectionScheduler | 每日2点 | 采集股票基本信息 |
| DataCollectionScheduler | 每日3点 | 采集基金基本信息 |
| DataCollectionScheduler | 每分钟 | 采集股票实时行情 |
| DataCollectionScheduler | 每15分钟 | 采集基金实时净值 |

### 4.3 API 响应格式

```java
public class ApiResponse<T> {
    private boolean success;
    private String message;
    private T data;

    public static <T> ApiResponse<T> success(T data)
    public static <T> ApiResponse<T> success(String message, T data)
    public static <T> ApiResponse<T> error(String message)
}
```

---

## 5. 数据模型

### 5.1 核心实体关系

```
┌─────────────────┐     ┌─────────────────┐
│   Stock         │     │    Fund         │
│   (聚合根)       │     │   (聚合根)       │
├─────────────────┤     ├─────────────────┤
│ symbol (code)   │     │ fundCode        │
│ name            │     │ name            │
│ industry        │     │ type            │
│ market          │     │ manager         │
└────────┬────────┘     └────────┬────────┘
         │                        │
         ▼                        ▼
┌─────────────────┐     ┌─────────────────┐
│  StockQuote     │     │   FundQuote    │
├─────────────────┤     ├─────────────────┤
│ stockId (FK)   │     │ fundId (FK)    │
│ timestamp       │     │ nav             │
│ open/high/low/close │  │ accNav         │
│ volume/amount   │     │ change/chg%    │
└─────────────────┘     └─────────────────┘

┌─────────────────┐     ┌─────────────────┐
│  PriceAlert     │     │ UserSubscription│
│   (聚合根)       │     │   (聚合根)       │
├─────────────────┤     ├─────────────────┤
│ userId          │     │ userId          │
│ symbol          │     │ symbol          │
│ symbolType      │     │ symbolType      │
│ alertType       │     │ targetChange%   │  -- 订阅时的阈值设置
│ targetPrice     │     │ status          │  -- ACTIVE/INACTIVE
│ targetChange%   │     │ createdAt       │
│ status          │     └────────┬────────┘
└────────┬────────┘              │
         │                       ▼
         │              ┌─────────────────┐
         │              │  RiskAlert     │
         │              │   (独立记录)     │
         │              ├─────────────────┤
         ▼              │ userId          │
┌─────────────────┐     │ symbol          │
│ AlertHistory   │     │ symbolType      │
├─────────────────┤     │ alertDate       │
│ alertId (FK)   │     │ timePoint       │  -- 11:30 / 14:30
│ symbol         │     │ hasRisk         │  -- 是否触发风险
│ symbolType     │     │ changePercent   │
│ triggeredPrice │     │ currentPrice    │
│ triggeredAt    │     │ yesterdayClose  │
│ message        │     │ isRead          │
└─────────────────┘     │ triggeredAt     │
                       └─────────────────┘
```

**说明：**
- `UserSubscription`：用户订阅关系表，存储用户订阅的标的及阈值设置
- `RiskAlert`：风险提醒记录表，每个时间点独立记录用户是否触发了风险
- 展示时，对同一标的取当日最新一条记录

### 5.2 数值精度规范

| 字段类型 | 精度 | 数据库类型 | 示例 |
|----------|------|------------|------|
| 股本 (totalShare/floatShare) | 2位小数 | `DECIMAL(20,2)` | 1000000.00 |
| 市盈率/市净率 (pe/pb) | 2位小数 | `DECIMAL(10,2)` | 15.50 |
| 价格 (price) | 2位小数 | `DECIMAL(10,2)` | 25.60 |
| 净值 (nav) | 4位小数 | `DECIMAL(10,4)` | 1.2345 |
| 涨跌幅 (changePercent) | 2位小数 | `DECIMAL(10,2)` | 5.25 |
| 金额 (amount) | 2位小数 | `DECIMAL(20,2)` | 5000000.00 |

---

## 6. API 接口规范

### 6.1 提醒接口

#### 创建提醒
```
POST /api/alerts
Content-Type: application/json

Request:
{
  "userId": 1,
  "symbol": "600519",
  "symbolType": "STOCK",
  "symbolName": "贵州茅台",
  "alertType": "PRICE_ABOVE",
  "targetPrice": 2000.00,
  "status": true
}

Response:
{
  "success": true,
  "message": "创建完成",
  "data": {
    "alert": { ... }
  }
}
```

#### 批量创建提醒 V2
```
POST /api/alerts/batch/v2
Content-Type: application/json

Request:
{
  "userId": 1,
  "symbolType": "STOCK",
  "symbols": ["600519", "000858", "600036"],
  "symbolNames": ["贵州茅台", "五粮液", "招商银行"],
  "alertType": "PERCENTAGE_CHANGE",
  "targetChangePercent": 1.0
}

Response:
{
  "success": true,
  "message": "批量创建完成",
  "data": {
    "batchId": "batch_xxx",
    "totalCount": 3,
    "successCount": 3,
    "failureCount": 0,
    "successList": [...],
    "failureList": []
  }
}
```

### 6.2 风险提醒接口

#### 获取用户风险提醒（按日聚合展示）
```
GET /api/risk-alerts/user/{userId}?page=1&pageSize=20

Response:
{
  "success": true,
  "data": {
    "records": [
      {
        "symbol": "600519",
        "symbolType": "STOCK",
        "symbolName": "贵州茅台",
        "date": "2024-04-14",
        "maxChangePercent": 3.5,
        "latestChangePercent": 2.1,
        "currentPrice": 1680.00,
        "yesterdayClose": 1646.00,
        "isRead": false,
        "latestTriggeredAt": "2024-04-14T14:30:00",
        "timePoints": ["11:30", "14:30"],
        "details": [
          {
            "timePoint": "11:30",
            "changePercent": 1.2,
            "currentPrice": 1666.00,
            "triggeredAt": "2024-04-14T11:30:00",
            "hasRisk": true
          },
          {
            "timePoint": "14:30",
            "changePercent": 2.1,
            "currentPrice": 1680.00,
            "triggeredAt": "2024-04-14T14:30:00",
            "hasRisk": true
          }
        ]
      }
    ],
    "total": 100,
    "size": 20,
    "current": 1,
    "pages": 5
  }
}
```

**说明**：同一标的同一日期可能有多条记录（11:30和14:30），查询时按日期聚合，展示时取最新一条记录。

#### 批量订阅（创建用户订阅）
```
POST /api/risk-alerts/subscribe
Content-Type: application/json

Request:
{
  "userId": 1,
  "symbolType": "STOCK",
  "symbols": ["600519", "000858"],
  "symbolNames": ["贵州茅台", "五粮液"],
  "targetChangePercent": 1.0,
  "status": true
}

Response:
{
  "success": true,
  "data": {
    "successCount": 2,
    "failureCount": 0
  }
}
```

**说明**：订阅接口创建的是 `user_subscription` 记录，定时任务根据这些订阅记录检测风险。

---

## 7. 部署架构

### 7.1 前端部署

**开发环境：**
- Vite Dev Server: `http://127.0.0.1:8080`
- HTTPS with self-signed cert
- Proxy to backend: `https://localhost:9090`

**生产环境：**
- 构建输出: `app/dist/`
- 静态文件部署到 CDN 或 Nginx

### 7.2 后端部署

**配置：**
- JDK 21
- 端口: 9090 (默认)
- 数据库: PostgreSQL
- 缓存: Caffeine (本地缓存)

**启动命令：**
```bash
cd backend
./mvnw spring-boot:run
# 或
java -jar target/data-collector-1.0-SNAPSHOT.jar
```

### 7.3 数据采集目标

**采集目标表 (data_collection_target)：**
- 支持股票和基金两种类型
- 可设置激活/停用状态
- 记录最后采集时间

---

## 8. 关键业务逻辑

### 8.1 风险提醒生成规则

1. **触发时间**：每个工作日的 11:30 和 14:30
2. **监控标的**：用户通过 `user_subscription` 表订阅的股票/基金
3. **检测条件**：
   - 基于用户订阅时设置的 `target_change_percent` 阈值
   - 计算涨跌幅 = |当前价格 - 昨日收盘价| / 昨日收盘价 * 100%
   - 涨跌幅 >= 目标百分比时触发
4. **记录规则**：
   - 每个时间点独立判断，有风险才记录
   - `risk_alert` 表按 (user_id, symbol, alert_date, time_point) 去重
   - 同一标的同一日期只有一条记录（更新最新）
   - 汇总显示取时间靠后的那条

### 8.2 提醒触发流程

```
1. RiskAlertScheduler 触发 (11:30 或 14:30)
       │
       ▼
2. RiskAlertAppService.checkAndCreateRiskAlerts(timePoint)
       │
       ▼
3. 获取所有激活的用户订阅 (user_subscription WHERE status = ACTIVE)
       │
       ▼
4. 对每个订阅标的获取当前价格
       │
       ▼
5. 计算涨跌幅，判断是否 >= 订阅时设置的阈值
       │
       ▼
6. 达到阈值 → 创建/更新 RiskAlert 记录
       │
       ▼
7. 风险提醒入库，用户可查看
```

### 8.3 数据模型说明

#### UserSubscription 表（新增）
用于存储用户的订阅关系，是风险提醒检测的数据源。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| user_id | BIGINT | 用户ID |
| symbol | VARCHAR(20) | 标的代码 |
| symbol_type | VARCHAR(20) | 标的类型：STOCK / FUND |
| symbol_name | VARCHAR(100) | 标的名称 |
| target_change_percent | DECIMAL(10,2) | 目标涨跌幅阈值 |
| status | VARCHAR(20) | 订阅状态：ACTIVE / INACTIVE |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

#### RiskAlert 表（新增/独立存储）
用于存储每次风险检测的结果记录，每条记录代表一个时间点的检测结果。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| user_id | BIGINT | 用户ID |
| symbol | VARCHAR(20) | 标的代码 |
| symbol_type | VARCHAR(20) | 标的类型：STOCK / FUND |
| symbol_name | VARCHAR(100) | 标的名称 |
| alert_date | DATE | 风险日期 |
| time_point | VARCHAR(10) | 时间点：11:30 / 14:30 |
| has_risk | BOOLEAN | 是否触发风险 |
| change_percent | DECIMAL(10,2) | 涨跌幅 |
| current_price | DECIMAL(10,2) | 当前价格 |
| yesterday_close | DECIMAL(10,2) | 昨日收盘价 |
| is_read | BOOLEAN | 是否已读 |
| triggered_at | TIMESTAMP | 触发时间 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

**去重索引**：`UNIQUE(user_id, symbol, alert_date, time_point)`

---

## 9. 项目启动

### 9.1 前端启动

```bash
cd app
npm install
npm run dev
# 访问 http://127.0.0.1:8080
```

### 9.2 后端启动

```bash
cd backend
./mvnw spring-boot:run
# 或确保 PostgreSQL 运行后打包运行
./mvnw package
java -jar target/data-collector-1.0-SNAPSHOT.jar
```

### 9.3 数据库要求

- PostgreSQL 数据库
- 执行初始化 SQL 脚本创建表结构
- 配置数据库连接信息

---

## 10. 相关文档

| 文档 | 路径 | 说明 |
|------|------|------|

